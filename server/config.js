const dbConfig = {
    host: 'localhost',
    user: 'root',
    password: 'hxj20020323',
    database: 'touchfish'
}

const sqlPath = '../sql/db'

const host = 'localhost';

const port = '3000';



module.exports = {
    dbConfig,
    sqlPath,
    host,
    port
}
